// Greetings.js

// Import React library
import React from "react";

// Define Greetings functional component
const Greetings = () => {
  // Render a paragraph element with the text "Hello, World!"
  return <p>Hello, World!</p>;
};

// Export the Greetings component
export default Greetings;
